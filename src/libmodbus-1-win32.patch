diff -ur libmodbus-3.1.12.orig/src/modbus-tcp.c libmodbus-3.1.12/src/modbus-tcp.c
--- libmodbus-3.1.12.orig/src/modbus-tcp.c	2026-06-04 18:59:32.746436896 -0400
+++ libmodbus-3.1.12/src/modbus-tcp.c	2026-06-04 19:04:54.197424721 -0400
@@ -236,7 +236,11 @@
     /* Set the TCP no delay flag */
     /* SOL_TCP = IPPROTO_TCP */
     option = 1;
+#ifdef OS_WIN32
+    rc = setsockopt(s, IPPROTO_TCP, TCP_NODELAY, (char *)&option, sizeof(int));
+#else
     rc = setsockopt(s, IPPROTO_TCP, TCP_NODELAY, &option, sizeof(int));
+#endif
     if (rc == -1) {
         return -1;
     }
@@ -598,7 +602,11 @@
     }
 
     enable = 1;
+#ifdef OS_WIN32
+    if (setsockopt(new_s, SOL_SOCKET, SO_REUSEADDR, (char*)&enable, sizeof(enable)) == -1) {
+#else
     if (setsockopt(new_s, SOL_SOCKET, SO_REUSEADDR, &enable, sizeof(enable)) == -1) {
+#endif
         close(new_s);
         return -1;
     }
@@ -715,7 +723,11 @@
             continue;
         } else {
             int enable = 1;
+#ifdef OS_WIN32
+            rc = setsockopt(s, SOL_SOCKET, SO_REUSEADDR, (char*)&enable, sizeof(enable));
+#else
             rc = setsockopt(s, SOL_SOCKET, SO_REUSEADDR, &enable, sizeof(enable));
+#endif
             if (rc != 0) {
                 close(s);
                 if (ctx->debug) {
