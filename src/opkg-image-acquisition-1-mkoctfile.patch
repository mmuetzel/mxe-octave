Add modified 'configure.ac' that checks for mkoctfile as set in configure flags.

--- /dev/null	2025-05-16 22:14:42.104126323 +0200
+++ image-acquisition-0.3.3/src/configure.ac	2025-05-15 20:52:02.708314000 +0200
@@ -0,0 +1,64 @@
+#                                               -*- Autoconf -*-
+# Process this file with autoconf to produce a configure script.
+
+AC_PREREQ([2.69])
+AC_INIT([Octave-Forge image-acquisition package], [0.3.0], [andy.weber.aw@gmail.com])
+AC_CONFIG_HEADERS([config.h])
+
+# Checks for programs.
+AC_PROG_CXX
+AC_LANG(C++)
+
+AC_CHECK_PROG(MKOCTFILE, mkoctfile, mkoctfile)
+if test -z "$MKOCTFILE"; then
+  AC_MSG_ERROR([mkoctfile required to install $PACKAGE_NAME])
+fi
+
+AC_PATH_PROG([FLTK_CONFIG], [fltk-config], [no])
+
+if test $FLTK_CONFIG = "no"; then
+  AC_MSG_ERROR([fltk-config required to install $PACKAGE_NAME])
+else
+  LIBS="`$FLTK_CONFIG --ldflags` $LIBS"
+	AC_DEFINE([HAVE_FLTK], [1], [Define to 1 if you have fltk-config])
+fi
+
+# Checks for libraries.
+AC_CHECK_LIB(v4l2, v4l2_open, [], [])
+AC_CHECK_LIB(jpeg, jpeg_destroy_compress, [], [])
+AC_CHECK_LIB(mfplat, MFStartup, [
+  LIBS="-lmfplat -lmf -lmfreadwrite -lmfuuid -lshlwapi -lole32 -lksguid -lamstrmid $LIBS"], [])
+
+# Checks for header files.
+AC_HEADER_DIRENT
+AC_CHECK_HEADERS([fcntl.h stdlib.h string.h sys/ioctl.h sys/time.h])
+
+# check for v4l2 headers
+AC_CHECK_HEADERS([libv4l2.h linux/videodev2.h],[],[])
+
+# check for jpeg headers
+AC_CHECK_HEADERS([jpeglib.h],[],[])
+
+# check for Media Foundation headers
+AC_CHECK_HEADERS([mfapi.h],[],[])
+
+# check for fltk headers
+AC_CHECK_HEADERS([FL/Fl.H FL/Fl_Double_Window.H FL/fl_draw.H],[],[
+  AC_MSG_ERROR([unable to find the needed fltk headers])
+])
+
+# Checks for library functions.
+AC_FUNC_ERROR_AT_LINE
+AC_FUNC_MMAP
+AC_FUNC_REALLOC
+AC_CHECK_FUNCS([munmap select strerror])
+
+AC_CONFIG_FILES([Makefile])
+AC_OUTPUT
+
+AC_MSG_NOTICE([
+
+$PACKAGE_NAME is now configured with
+
+   LIBS:   $LIBS
+])
